[pytest]
pythonpath = . fastapi_project
testpaths = tests
# harmless deprecation inside starlette's TestClient (library code, not ours)
filterwarnings =
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
